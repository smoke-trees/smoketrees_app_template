import 'package:get/get.dart';

class InitBindings extends Bindings {
  @override
  void dependencies() {
    // Register app-wide controllers here, for example:
    // Get.put<AppSettingsController>(AppSettingsController(), permanent: true);
  }
}